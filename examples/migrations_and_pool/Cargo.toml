[package]
name = "migrations_and_pool"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
ducklake-orm = { path = "../../ducklake-orm", features = ["migrations"] }
duckdb = { workspace = true }
